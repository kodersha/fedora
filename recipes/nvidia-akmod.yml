modules:
  - type: dnf
    repos:
      cleanup: true
      nonfree: rpmfusion

    install:
      allow-erasing: true
      install-weak-deps: false
      skip-unavailable: true
      packages:
        - akmod-nvidia
        - xorg-x11-drv-nvidia
        - xorg-x11-drv-nvidia-cuda
